<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
  <style name="fragment_textviewone">
      <item name="android:layout_width">wrap_content</item>
      <item name="android:layout_height">wrap_content</item>
      <item name="android:textSize">@dimen/fragment_textview</item>
  </style>
    <!--弹出框的Style-->
    <style name="MyDialogStyle" parent="@android:style/Theme.Dialog">
        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 浮于Activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- Dialog以外的区域模糊效果 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- Dialog进入及退出动画 -->
        <item name="android:windowAnimationStyle">@style/ActionSheetDialogAnimation</item>
    </style>
    <!-- 进出动画 -->
    <style name="ActionSheetDialogAnimation" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/dialog_in</item>
        <item name="android:windowExitAnimation">@anim/dialog_out</item>
    </style>

    <style name="ActionSheetDialogStyle" parent="@android:style/Theme.Dialog">
        <!--背景透明-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <!--浮于Activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--边框-->
        <item name="android:windowFrame">@null</item>
        <!--Dialog以外的区域模糊效果-->
        <item name="android:backgroundDimEnabled">true</item>
        <!--无标题-->
        <item name="android:windowNoTitle">true</item>
        <!--半透明-->
        <item name="android:windowIsTranslucent">true</item>
        <!--&lt;!&ndash;Dialog进入及退出动画&ndash;&gt;-->
        <item name="android:windowAnimationStyle">@style/ActionSheetDialogAnimation</item>
    </style>
    <!--这是影的对话框的出入动画-->
    <style name="ActionSheetDialogAnimation_ying" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/actionsheet_dialog_in</item>
        <item name="android:windowExitAnimation">@anim/actionsheet_dialog_out</item>
    </style>

    <!--这是继承ActionSheetDialogAnimation，只需要重写出入动画项目，用在头像对话框的-->
    <style name="fragment4_toolbar_button_headportrait_dialog" parent="ActionSheetDialogStyle">
        <item name="android:windowAnimationStyle">@style/fragment4_toolbar_button_headportrait_dialog_Animation</item>
    </style>
    <!--这是头像对话框出入的动画-->
    <style name="fragment4_toolbar_button_headportrait_dialog_Animation" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/fragment4_toolbar_button_headportrait_dialog_in</item>
        <item name="android:windowExitAnimation">@anim/fragment4_toolbar_button_headportrait_dialog_out</item>
    </style>

    <!--编辑框超出规定的字数的样式-->
    <style name="MyOverflowText" parent="Base.TextAppearance.AppCompat.Small">
        <item name="android:textColor">#f3672b</item>
    </style>
    <style name="MyErrorStyle">
        <item name="android:textColor">#ec4722</item>
    </style>




</resources>
